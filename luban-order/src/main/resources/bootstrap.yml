spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-dataids: mall-nacos.yml
        refreshable-dataids: mall-nacos.yml
  application:
    name: mall-order
  profiles:
    active: dev